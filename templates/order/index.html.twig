
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Creation de la commande{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22></text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
    {% include '_partials/_flash.html.twig' %}
    
    <!-- Navigation-->
       <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container px-4 px-lg-5">
                <a class="navbar-brand" href="\">Commencer a Shopper</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                        <li class="nav-item"><a class="nav-link active" aria-current="page" href="boutique">Produits</a></li>
                       
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Autre</a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <li><a class="dropdown-item" href="/connexion">Connexion</a></li>
                                <li><hr class="dropdown-divider" /></li>
                                <li><a class="dropdown-item" href="/inscription">Inscription</a></li>
                                <li><a class="dropdown-item" href="/order/create">Order</a></li>
                            </ul>
                        </li>
                        
                    </ul>
                    <form class="d-flex">
                        <button class="btn btn-outline-dark" type="submit">
                            
                             <a class="dropdown-item" href="/mon-panier">Mon Panier</a>
                            
                        </button>
                    </form>
                </div>
            </div>
        </nav>
        <!-- Header-->
        
        <!-- Section-->
       

        {% include '_partials/_flash.html.twig' %}
        

       
        
    </body>
</html>



{% block body %}
       <div class='a' style='width:52.5%;'></div>
       <div class='b'>
          <div class='c'>
             <div class='d'>
                 <nav aria-label='breadcrumb'>
                    <ol class='e'>
                        
                        
                     </ol>
                 </nav>
                 {{ form_start(form, {action:path('order_prepare')}) }}
                 
                 <h4 style="margin:10px">1.
                     <span style="color: #4CAF50;">Détails de livraison</span>
                 </h4>
                 <p style="margin:10px">Si vous n'avez d'adress ajouter en une dans votre espace compte<a href='{{ path('app_register')}}' class="btn btn-primary" >Ajouter une adresse</a>
                 </p>
                 <h4 style="margin:10px">2.<span style="margin:3px ; color: #4CAF50;">Votre adresse</span><h4>
                 {{ form_row(form.addresses) }}<br>
                 <h4 style="margin:10px">3.<span style="margin:3px ; color: #4CAF50;">Mode de livraison</span></h4>
                 {{ form_row(form.transporter) }}<br>



              </div>
              <div>
              
                 <h5 style="margin:10px ">Recapitulatif de la commande<span>({{ recapCart|length }} type(s) de produits )</span></h5>
                 {% set total=null %}
                 {% for item in recapCart %}
                 {% set total=total + (item.product.price * item.quantity) %}
                    <div>
                    <h6 style="margin:10px">{{ item.product.title }}</h6>
                       <img src='{{ vich_uploader_asset(item.product,'attachementFile') }}' width='110' alt='{{item.product.title }}' style="margin:10px;border-radius: 30%;width: 150px; height: 140px;"></a>
                     <div>
                        <div>
                           <div style='position: relative; top: 15px;'>
                              
                              <p style="margin:10px">{{ item.product.subtitle }}</p>
                            </div>
                            <div>
                               <h6 style="margin:10px"> Quantite : x{{item.quantity}} <br> Prix du produit: {{ (item.product.price / 100)|number_format(2,',','.') }} €</h6>
                            </div>
                        </div>
                        <div>
                           <a href='{{ path('cart_remove', {id:item.product.id}) }}' data-bs-toggle='tooltip' title='Remove'>
                              
                            </a><br>
                         </div>
                     </div>
                </div>

                {% endfor %}
                 <div> <h6 style="margin:10px">Total: </h6> <h6 style="margin:10px">{{ (total / 100 )| number_format(2,',','.')  }} € <strong>plus le prix de mode de livraison que tu est choisise</h1></h6>
                 </div>
                 <div>
                     
                     <button type='submit'  class="btn btn-lg btn-primary w-100 w-dm-auto mt-5" style = "display: inline-block;
    padding: 10px 20px;
    background-color: #4CAF50;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 16px;
    transition: background-color 0.3s;
    background-color: #45a049;">Valider votre commande</button>
                 </div>
              </div>
              {{ form_end(form) }}
       </div>
<br><br>
       <footer class="py-5 bg-dark">
            <div class="container"><p class="m-0 text-center text-white">Copyright &copy; Your Website 2023</p></div>
        </footer>
        <!-- Bootstrap core JS-->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
        <!-- Core theme JS-->
        <script src="js/scripts.js"></script>

        

        

{% endblock %}
